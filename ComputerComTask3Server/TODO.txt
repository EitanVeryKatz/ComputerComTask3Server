updated TODO:
Gal: add wireshark pics
Eitan: remove unnecessary couts to the console (so it would be cleaner, only prints that are comming from the queries handles itself such as POST prints, PUT prints or DELETE prints to the screen should be acceptable)